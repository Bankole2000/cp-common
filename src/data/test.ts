// import faIcons from './faIcons.json';
// import mdiIcons from './mdiIconsData.json';

// const { length } = Object.keys(faIcons);
// const def = Object.keys(faIcons)[length - 1];
// console.log(faIcons[def as unknown as any]);
// export const faIconsList = faIcons;
// const mdiIconNames = mdiIcons.map((icon) => icon.name);
// export const mdiIconsList = mdiIconNames;
// export const mdiIconsData = mdiIcons;

// console.log({ faIcons, type: typeof faIcons, mdiIconNames });

// const { length: faIconsLength } = Object.keys(faIcons);
// const fadef = Object.keys(faIcons)[faIconsLength - 1];
// export const faIconsList = faIcons[fadef as unknown as any];

// const { length: mdiIconsLength } = Object.keys(mdiIcons);
// const mdidef = Object.keys(mdiIcons)[mdiIconsLength - 1];
// export const mdiIconsRaw = mdiIcons[mdidef as unknown as any];
// console.log({ faIconsList, mdiIconsRaw });
